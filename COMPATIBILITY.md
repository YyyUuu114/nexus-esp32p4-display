# 版本与端点兼容规则

本文是 NEXUS 固件与 Windows 桌面端的规范性兼容性定义。每个发布提交必须包含与实现一致的 `release.json`；`compatibility.json` 提供机器可读的全局规则。

## 1. 产品版本

产品版本格式为 `MAJOR.LINE.CHANNEL`。

- `MAJOR`：产品与线协议的破坏性兼容代际。协议发生不兼容变更时必须递增。
- `LINE`：功能发布线。正式发布按 `1.1.0`、`1.2.0`、`1.3.0` 递增。
- `CHANNEL`：发布通道。`0` 仅用于正式版；`1` 至 `9` 仅用于开发版。

开发版示例为 `1.2.1` 至 `1.2.9`，其目标正式版为 `1.2.0`。因此同一发布线内，`.0` 表示完成验证的最终正式版，发布优先级高于 `.1` 至 `.9`。该排序规则不同于通用 SemVer 的数值排序；更新器必须按通道隔离清单，禁止使用普通数值比较在开发版与正式版之间自动切换。

## 2. 线协议版本

线协议版本格式为 `WIRE_MAJOR.WIRE_REVISION`。

- `WIRE_MAJOR` 由 JSON 字段 `v` 承载。不同 `WIRE_MAJOR` 的端点不兼容。
- `WIRE_REVISION` 记录同一主版本内的增量能力，不要求在每帧中发送；端点能力由发布元数据声明。
- 同一 `WIRE_MAJOR` 内，发送端必须允许可选字段缺失；接收端必须忽略未知字段。
- 字段改名、删除、类型改变、单位改变、空值语义改变、帧边界改变或传输语义改变均属于破坏性变更，必须递增 `WIRE_MAJOR`。

## 3. 更新分类

| `updateClass` | 适用条件 | 协议版本 | 另一端要求 |
| --- | --- | --- | --- |
| `single-endpoint` | UI、性能、日志、部署或内部实现变化，线协议没有变化 | 不变 | 不需要更新 |
| `coordinated-additive` | 新增可选字段或能力；旧端可安全忽略或缺省 | 主版本不变，修订号递增 | 为使用新能力建议同步更新，但不是兼容性前提 |
| `coordinated-breaking` | 删除、重命名或改变既有字段/单位/传输语义 | 主版本递增 | 必须同时更新，混用版本视为不兼容 |
| `paired-baseline` | 首次建立可验证的成对发布基线 | 由基线声明 | 首次部署必须同时安装声明版本 |

每次发布说明、`CHANGELOG.md` 和 `release.json` 必须使用同一分类，并明确 `peerUpdateRequired`。

## 4. 运行时兼容判定

一对端点仅在下列条件全部成立时受支持：

1. 两端声明相同的 `WIRE_MAJOR`。
2. 固件的桌面端支持范围包含实际桌面端产品版本。
3. 桌面端的固件支持范围包含实际固件产品版本。
4. 若任一端发布元数据声明 `peerUpdateRequired=true`，另一端必须满足该发布指定的配对版本。

`WIRE_REVISION` 不相等时，只允许使用双方共同支持的字段集合。较新端不得把新增字段设为旧端运行所必需。

## 5. 当前基线

| 端点 | 产品版本 | 通道 | 线协议 | 支持的另一端 |
| --- | --- | --- | --- | --- |
| ESP32-P4 固件 | 1.1.0 | stable | 1.1 | Desktop 1.1.0 |
| Windows 桌面端 | 1.1.0 | stable | 1.1 | Firmware 1.1.0 |

v1.1.0 的分类为 `paired-baseline`。后续仅优化某一端且线协议保持 1.1 时，应标记为 `single-endpoint`；新增可选遥测字段时应标记为 `coordinated-additive` 并递增修订号；任何既有字段或单位的不兼容调整必须进入产品主版本 2 与线协议主版本 2，并成对发布。

当前固件开发分支为 v1.2.1，线协议仍为 1.1，更新分类为 `single-endpoint`，支持桌面端 v1.1.0 及其他声明兼容线协议 1 的版本。
