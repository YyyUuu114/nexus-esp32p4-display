# Changelog

本文件记录正式发布。开发分支的中间版本不构成稳定性承诺。

## 2.1.1 — development

发布分类：`coordinated-breaking`；线协议升级为 2.0，必须同步更新桌面端至 v2.1.1。

- 增加随机 nonce 双向握手、产品身份和双端产品版本范围校验，消除普通串口误连接。
- 要求每个遥测帧绑定已协商会话，并严格拒绝小数版号/序号、越界数值、尾随数据和内部 NUL。
- 15 秒空闲后停止背光、LVGL、触摸与 MIPI-DSI，同时保留原生 USB 唤醒能力；启用动态降频并保持自动 light sleep 关闭。
- 将分区表迁移到 `0x10000`、应用迁移到 `0x20000`，为 Bootloader 保留约 33 KiB 余量。
- 增加协议验证单元测试、开发分支 CI 和严格发布元数据校验。

## 1.1.0 — 2026-08-09

发布分类：`paired-baseline`；首次部署要求固件与桌面端同时使用 v1.1.0。

- 建立 ESP32-P4-Function-EV-Board 1024 × 600 MIPI-DSI 仪表界面。
- 支持 CPU、GPU、内存、网络、风扇、温度、功耗和会话累计能耗。
- 实现 60 秒负载曲线、数据陈旧状态和 15 秒无连接背光待机。
- 保持板载 ESP32-C6 复位并关闭未使用的扬声器功放使能。
- 建立 USB Serial/JTAG JSON 协议 1.1 和显式端点兼容规则。
