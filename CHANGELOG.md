# Changelog

本文件记录正式发布。开发分支的中间版本不构成稳定性承诺。

## 2.1.1 — development

发布分类：`coordinated-breaking`；线协议升级为 2.0，必须同步更新固件至 v2.1.1。

- 用随机 nonce 双向握手、产品身份与双端版本范围取代单串口回退，避免误连普通串口。
- 便携包首次运行安装到受保护的 Program Files；可选计划任务仅指向受保护程序，默认保持关闭。
- 在线更新使用内置 ECDSA P-256 公钥验证签名载荷，再校验 ZIP 哈希和安全解压路径，并通过 `current/rollback` 原子交换回滚。
- 固定显示 GPU 的选择，能耗对 CPU 与所有 GPU 的非负有效功耗积分，拒绝负值/异常值并保证累计值单调。
- 日志保持短单行、256 KiB 轮转和两份历史，并将过期清理周期缩短为 7 天。
- 增加版本、能耗、握手与签名验证测试以及开发分支 CI。

## 1.1.0 — 2026-08-09

发布分类：`paired-baseline`；首次部署要求桌面端与固件同时使用 v1.1.0。

- 提供 Windows 10/11 x64 自包含单文件应用与便携 ZIP。
- 自动识别 Espressif USB Serial/JTAG 端口并在断线后低频重连。
- 采集 CPU、GPU、内存、网络、风扇、温度和功耗，并累计进程会话能耗。
- 提供可选计划任务开机自启、托盘控制、单实例保护和短日志轮转。
- 提供带 HTTPS、通道校验、版本校验和 SHA-256 校验的一键更新流程。
- 建立 USB 遥测协议 1.1 和显式端点兼容规则。
